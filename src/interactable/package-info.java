/**
 * This package contains the interfaces and classes linked to the game's interactive elements.
 * <p>
 * Interactive elements can be enemies or objects with which the
 * characters can interact with. The {@link interactable.Interactable} interface defines the method
 * interaction method for these elements, allowing characters to interact with the
 * game world dynamically.
 * </p>
 */
package interactable;